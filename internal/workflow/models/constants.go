package models

// CurrentVersion is the version of the action object
const CurrentVersion = 1

// LockDomainActionType is the action type for update locks workflow
const LockDomainActionType = "LockDomain"

// UnlockDomainActionType is the action type for update locks workflow
const UnlockDomainActionType = "UnlockDomain"
